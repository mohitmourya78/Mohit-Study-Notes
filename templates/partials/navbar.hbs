<div class="backgroundcolor">

    <nav class="navbar respnavh">
        <div class="logo">
            <a href="home"><img src="img/mn-initial-letters-logo-design-free-vector.jpg" alt="logo"></a>
        </div>

        <ul class="navlist respnavv">

            <li><a href="/">Home</a></li>
            <li><a href="about">About</a></li>
            <li><a href="contactt">Contact Us</a></li>
            <li><a href="blog">Blog</a></li>
            <li><a href="/courses">Courses</a></li>
            <li class="menu-item dropdown"><a>Tutorial &dtrif;</a>
                <ul class="dropdown">
                    <li><a href="#">Web Devlopment</a></li>
                    <li><a href="#">Full Stack Development</a></li>
                    <li><a href="#">Fronted Devlopment</a></li>
                    <li><a href="#">Data Analytics</a></li>
                    <li><a style="color: blue;" href="/courses">More</a></li>
                  </ul>
            </li>
            <li><a href="/notes">Notes</a></li>
            <li><a href="contactt">Work With Us</a></li>

        </ul>

        <div class="burger" id= "burger">
            <div class="line"></div>
            <div class="line"></div>
            <div class="line"></div>
        </div>
        
        <div class="rightnav">
            <div class="logsign" id="authLinks">

              

            <div class="logsign" id="authLinks">
                {{#if isLoggedIn}}
                    <li class="login account" id="logout"><a href="/logout">Log Out</a></li>
                {{else}}
                    <div id="loginacc" style="display:flex; gap:5px;">
                        <li class="login account"><a href="/login" target="_blank">Log In</a></li>
                        <li class="signup account"><a href="/register" target="_blank">Sign Up</a></li>
                    </div>
                {{/if}}
            </div>
        </div>
            <div class="search-container" id="search-container">
                <input type="text" placeholder="Search.." class="search-input" id="search-input">
                <div class="search-icon" id="search-icon" onclick="searchwarning()">
                    <!-- You can use any search icon here -->
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24">
                        <path
                            d="M20.7 19l-5.2-5.2c1.2-1.4 1.9-3.3 1.9-5.3 0-4.4-3.6-8-8-8s-8 3.6-8 8 3.6 8 8 8c2 0 3.9-0.7 5.3-1.9l5.2 5.2c0.4 0.4 1 0.4 1.4 0 0.4-0.4 0.4-1 0-1.4zM4 10c0-3.3 2.7-6 6-6s" />
                    </svg>
                </div>
            </div> 
        </div>
     
    </nav>


</div>

<button id="scrollToTopBtn" class="scroll-to-top-btn">&#x2191;</button>


<div class="mainHeading">


<section class="heading ">
    <h1>
        Welcome To <span style="color: rgb(126 34 206); line-height: 2.5rem; font-weight: 600;">Mohit Notes</span>
    </h1>
    
    <h1> Learn
        <a href="" class="typewrite" data-period="2000" data-type='[ "HTML.", "CSS.", "JavaScript.", "ReactJs." , "NodeJs" , "DevOps" , "MongoDB" , "Full Stack" , "Fronted Development"]' style="text-decoration:none;">
          <span class="wrap"></span>
        </a>
      </h1>

    <p>Get notes in our website in free of cost , with easy language and hand written notes click below. This is my attempt to teach basics and those coding techniques to people in short time which took me ages to learn.
    </p>
    <div style="display:flex; gap: 3px;">
        <button class="fstbtn" onclick="redirectToGrid()" >
            Explore Notes
        </button>
        <button class="fstbtn signup bigbtn" onclick="redirectToRegister()" >
            SignUp
        </button>
    </div>
</section>

<div class="backgroundimg">

</div>

</div>


<script>

    function redirectToGrid() {
        window.location.href = "home#scrollTarget";
    }

    function redirectToRegister() {
        window.location.href = "/register";
    }

    document.getElementById('burger').addEventListener('click', function() {
        this.classList.toggle('active');
    });

    const textInput = document.getElementById("search-input");
    const searchContainer = document.getElementById("search-container");
    
    document.addEventListener("click", function(event) {
        if (!event.target.closest("#search-container")) {
            textInput.placeholder = "Search..";
            textInput.classList.remove("placeholder-red");
        }
    });
    
    function searchwarning() {
        if (textInput.value === '') {
            textInput.placeholder = "What you want? Type here..!";
            textInput.classList.add("placeholder-red");
        } else {
            textInput.placeholder = "Search..";
            textInput.classList.remove("placeholder-red");
        }
    }
    

    var TxtType = function(el, toRotate, period) {
        this.toRotate = toRotate;
        this.el = el;
        this.loopNum = 0;
        this.period = parseInt(period, 10) || 2000;
        this.txt = '';
        this.tick();
        this.isDeleting = false;
      };
      
      TxtType.prototype.tick = function() {
        var i = this.loopNum % this.toRotate.length;
        var fullTxt = this.toRotate[i];
      
        if (this.isDeleting) {
        this.txt = fullTxt.substring(0, this.txt.length - 1);
        } else {
        this.txt = fullTxt.substring(0, this.txt.length + 1);
        }
      
        this.el.innerHTML = '<span class="wrap">'+this.txt+'</span>';
      
        var that = this;
        var delta = 200 - Math.random() * 100;
      
        if (this.isDeleting) { delta /= 2; }
      
        if (!this.isDeleting && this.txt === fullTxt) {
        delta = this.period;
        this.isDeleting = true;
        } else if (this.isDeleting && this.txt === '') {
        this.isDeleting = false;
        this.loopNum++;
        delta = 500;
        }
      
        setTimeout(function() {
        that.tick();
        }, delta);
      };
      
      window.onload = function() {
        var elements = document.getElementsByClassName('typewrite');
        for (var i=0; i<elements.length; i++) {
            var toRotate = elements[i].getAttribute('data-type');
            var period = elements[i].getAttribute('data-period');
            if (toRotate) {
              new TxtType(elements[i], JSON.parse(toRotate), period);
            }
        }
        var css = document.createElement("style");
        css.type = "text/css";
        css.innerHTML = ".typewrite > .wrap { border-right: 0.08em solid #fff}";
        document.body.appendChild(css);
      };


      document.addEventListener('DOMContentLoaded', () => {
        const scrollToTopBtn = document.getElementById('scrollToTopBtn');
      
        window.addEventListener('scroll', () => {
          if (document.body.scrollTop > 100 || document.documentElement.scrollTop > 100) {
            scrollToTopBtn.style.display = 'block'; 
          } else {
            scrollToTopBtn.style.display = 'none'; 
          }
        });
      
        scrollToTopBtn.addEventListener('click', () => {
          window.scrollTo({
            top: 0,
            behavior: 'smooth' // Smooth scroll to the top
          });
        });
      });
      


</script>


